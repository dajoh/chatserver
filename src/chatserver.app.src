{application, chatserver, [
	{description, "A websocket chat server with a web interface."},
	{vsn, "1"},
	{modules, []},
	{registered, [chatserver_sup, chatserver_room, chatserver_web]},
	{applications, [
		kernel,
		stdlib,
		cowboy
	]},
	{mod, {chatserver_app, []}},
	{env, []}
]}.
